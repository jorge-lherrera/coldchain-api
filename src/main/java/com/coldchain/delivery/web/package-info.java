/**
 * Every controller, grouped by module. A controller speaks HTTP and calls the module's published
 * contract; it belongs to the delivery adapter and never to the module it serves.
 */
package com.coldchain.delivery.web;
