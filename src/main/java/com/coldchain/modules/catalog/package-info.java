/**
 * Storage profiles, products and sites. The reference data a shipment is described with.
 */
@ApplicationModule(id = "catalog", allowedDependencies = {})
package com.coldchain.modules.catalog;

import org.springframework.modulith.ApplicationModule;
