/**
 * The composition root: the one place that says which modules make up this application.
 */
@ApplicationModule(displayName = "Bootstrap", type = ApplicationModule.Type.OPEN)
package com.coldchain.bootstrap;

import org.springframework.modulith.ApplicationModule;
