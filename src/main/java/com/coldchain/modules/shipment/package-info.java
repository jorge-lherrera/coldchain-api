/**
 * The shipment, its state machine, the two-step handoff and the hash-chained custody log.
 */
@ApplicationModule(id = "shipment", allowedDependencies = {})
package com.coldchain.modules.shipment;

import org.springframework.modulith.ApplicationModule;
